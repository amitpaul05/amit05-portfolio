---
date: 2026-04-29
moods: [focused, relaxed]
---

Today I worked on a chat sync issue caused by token expiry between two auth systems. Traced the flow and added a small JWT expiry check so tokens refresh before API calls instead of failing first. Sync runs asynchronously after DB writes, so no user-facing delay. Kept it simple and avoided adding background workers.

Built a streaming CSV export for large conversation data. Used generator-based response to avoid loading everything into memory, reduced query size, and grouped related data to avoid N+1 queries.

Also added search to conversation list and cleaned up serializer usage to include extra metadata properly.

After office: read graph traversal and solved some LeetCode.