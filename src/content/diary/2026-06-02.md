---
date: 2026-06-02
moods: [motivated]
---

Today I hit a tricky regression in the subscription system where a refactor silently broke tier validation by replacing backend feature keys with UI-only metadata. I traced the mismatch and restored the original functional contract while keeping the UI layer cleanly separated.

I also extended the participant invitation flow by adding resend support and exposing invitation links in the list API, making sure expiry and acceptance states still behave correctly under all edge cases while keeping the query layer efficient.

After office hours, I explored Redis by building an OTP flow, focusing on TTL-based expiry, single-use behavior, and securing storage using HMAC instead of raw values, along with proper key namespacing to keep authentication flows isolated.