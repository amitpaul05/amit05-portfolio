---
date: 2026-08-18
moods: [focused]
---

A company_admin account kept getting a 403 on our new documents API while the same token worked on every other endpoint. The account was fine and so were the migrations, because a superuser could upload without trouble. Every API we added before this one was a new endpoint on an existing model, so its group permissions were already in place. This time it was a whole new app, so migrate created fresh permission rows and nothing ever attached them to a group.

Curl settled it: documents came back 403 while manuals, answers and audits returned 200 on the same token. The real bug lived in our deploy script, which runs migrations on each release and nothing else, so t run since someone last did itby hand. All four environments share that script, so I checked the blast radius beforetouching it, then added the coy task. It runs once per release and fails the deploy loudly instead of crash-looping a live service.                  
The rest of the day went to the document linking API. One call now attaches a documentany number of requirements andsingle transaction, with everytarget checked before the first write. A requirement with no answer gets one created aundeclared, so it stays open ae denominator instead of quietly improving someone's score.                                                            
Chasing that turned up an audit reporting 125% completion, where the query counted ansthrough their evidence files, ounted twice. I also closed agap where a PATCH could attach another company's file to a manual. Ended on 272 tests green.

Neither bug was where the erro a deploy script and the otherwas in a join.