---
date: 2026-05-05
moods: [relaxed]
---

Today I focused mostly on cleaning and stabilizing parts of the system. Refactored the CSV export flow into a shared manager so both single and bulk exports follow a consistent path, which removed a lot of duplication. Also tightened the behavior around edge cases so exports don’t silently succeed with empty data anymore.

On the infra side, got the full project running through Docker Compose. Faced a couple of startup issues (Django checks before migrations, SDK init at import time), but handled them at the container level without touching application logic. Now the setup is stable and reproducible for fresh deployments.