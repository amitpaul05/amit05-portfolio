---
date: 2026-07-28
moods: [motivated, tired]
---

This style fits your goal well. Don't write "Today I implemented X." Instead, write the story of what problem you inherited, how you reasoned, what decision you made, how you verified it, and what you learned. That's exactly what interviewers look for.

Keep each entry to 2–4 short sentences.

Instigate
Deployment Pipeline

Expanded our deployment pipeline from two environments to three without changing the application itself. Spent most of the day understanding how Cloudflare Pages, environment files, and build patches fit together before touching the pipeline. It reminded me that deployment architecture is mostly about reducing risk, not adding steps.

Infrastructure Review

Before making infrastructure changes, I audited the deployment flow and wrote down every operational risk I could find. Interesting how a few missing safeguards can become bigger problems than the code itself. Good reminder to understand a system before improving it.

Development Environment

Local setup fought me more than expected. Fixed Node compatibility issues and tracked an Expo Router build problem that only appeared during bundling. Small configuration changes ended up making the build both stable and lighter.

Django + Supabase

Designed a Django integration without taking ownership away from Supabase. The challenge was fitting into an existing architecture instead of replacing it. Learned that respecting system boundaries is often the better engineering decision.

Code Review

Reviewed a large feature branch before thinking about implementation details. Writing down the architecture in my own words helped me understand the system much faster. Finished the day by verifying deployments instead of assuming they worked.

Govara
Importer Investigation

Started with a bug report that looked like database corruption. Followed the data step by step until the real problem turned out to be duplicate article codes in the uploaded framework. Nice reminder that evidence beats assumptions.

Reporting Optimization

A planned redesign sounded expensive, so I looked for a simpler path first. Reused the existing reporting flow instead of introducing new infrastructure and confirmed query performance stayed the same. Sometimes the best solution is the smallest one.

General Office Examples
Debugging

Spent most of today reading code instead of writing it. Understanding why something exists is usually faster than replacing it later.

Performance

Looked into API performance today. The improvement itself was useful, but learning where the actual bottleneck lived was even more valuable.

Database

Investigated a database query that looked harmless until I checked how it behaved with real data. Every optimization starts with measuring instead of guessing.

Deployment

Production deployments always remind me that reliability is a feature. A deployment isn't finished until I know I can safely repeat it.

Code Review

Reviewed someone else's changes today. I noticed good reviews are less about finding bugs and more about making future changes easier.

The tone is intentionally simple, reflective, and natural—like personal notes from an engineer rather than polished LinkedIn posts. Each entry subtly captures interview-worthy thinking (ownership, debugging, trade-offs, verification, and lessons learned) without sounding rehearsed.


Merged but separate paragraph. Keep only what I did, nothing more. Also user's Don't know about the context, so it should start with the project's short context. But a story format, like I am telling someone the full day activity of mine to someone

Yes. If the reader doesn't know the project, each entry should begin with one sentence of context, then naturally flow into what happened during your day. It should read like you're telling a colleague what you worked on after work—not like release notes or a résumé.

Here are examples based only on what you actually did.

Instigate – Deployment Pipeline
Instigate is an Expo application deployed on Cloudflare Pages with Supabase as the backend. Today I took ownership of extending our deployment pipeline from two environments to three. Most of the time went into understanding how the existing Cloudflare Pages setup, environment files, and build patches worked together before adding the new dev-dhaka stage. By the end of the day, the promotion flow was ready while keeping a clear migration path to a dedicated Supabase instance later.

Instigate – Infrastructure Audit
Instigate is still growing, so making deployments safer is becoming more important. Before changing anything, I spent the day reviewing how the current deployment process actually works. I traced the flow from build to production, noted the weak points around CI, deployments, backups, and migrations, and organized them into a prioritized roadmap instead of jumping straight into implementation.

Instigate – Local Environment
Instigate uses Expo and React Native, so getting a stable local environment is the first step before making platform changes. Today I focused on making the project reliable to work on locally. After resolving Node compatibility issues, I tracked down an Expo Router bundling problem that was blocking native builds and finished by reducing the production web bundle size through Metro configuration changes.

Instigate – Django Backend
We're introducing Django alongside Supabase without changing the existing database ownership. Today I worked on designing that integration carefully. I kept Supabase responsible for the schema, implemented JWT verification in Django, configured a bypass-RLS service role, and finished with a proof of concept that worked without affecting the production database.

Instigate – Review & Verification
A large integration branch introduced several architectural changes to Instigate. Instead of merging blindly, I spent the day understanding what had changed, documenting the architecture in a simpler way, and verifying the deployment from end to end. After running all 866 tests, I investigated a Cloudflare routing issue and updated the deployment runbook with everything I learned.

Govara – Compliance Import
Govara is a compliance platform where customers upload regulatory frameworks for assessment. Today I investigated a report that suggested imported data was being overwritten. After tracing the importer and validating the client datasets, I confirmed the issue came from duplicate article codes inside the uploaded framework rather than corruption in our import process.

Govara – Reporting
Govara generates compliance audit reports from imported framework data. Today I looked into a reporting redesign that was expected to take about a week. Instead of replacing the existing pipeline, I extended what was already there, kept the database queries efficient with optimized ORM loading, and verified everything still behaved correctly after the changes.


Both Merged you idiot 

You're right—I split them by activity. What you want is one continuous diary entry for the whole day, where everything flows together naturally.

For example:

Instigate is an Expo application deployed on Cloudflare Pages with Supabase as the backend. Today I took ownership of expanding our deployment pipeline from two environments to three by introducing a new dev-dhaka stage. Before changing anything, I spent time understanding how the existing Cloudflare Pages setup, environment files, and build patches worked together, then documented a safe migration path to a dedicated Supabase instance for the future. After that, I reviewed the overall deployment architecture, identified weak points around CI, deployments, backups, and migrations, and organized them into a prioritized roadmap. I also made the local development environment stable by resolving Node compatibility issues, fixed an Expo Router bundling problem that was blocking native builds, and reduced the production web bundle size through Metro configuration changes. The day ended with designing a Django integration that keeps Supabase as the schema authority, implementing JWT verification with a bypass-RLS role, reviewing a large integration branch, verifying deployments by running all 866 tests, investigating a Cloudflare routing issue, and updating the deployment runbook with everything I learned.

Govara is a compliance SaaS that helps organizations assess themselves against different security and regulatory frameworks. Today I started by investigating a report that suggested imported compliance data was being overwritten. After tracing the importer and validating the client datasets, I confirmed the issue came from duplicate article codes inside the uploaded framework rather than any corruption in our import process. Later, I worked on improving the audit reporting flow. Instead of replacing the existing pipeline with a larger redesign, I extended the current implementation, kept database query performance stable through optimized ORM loading, and finished by verifying that everything still behaved correctly after the changes.